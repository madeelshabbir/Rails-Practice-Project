<table>
  <tr>
    <th>Name</th>
    <th>Phone No</th>
    <th colspan="3"></th>
  </tr>

  <% @persons.each do |person| %>
    <tr>
      <td><%= person.name %></td>
      <td><%= person.number %></td>
    </tr>
  <% end %>
</table>
